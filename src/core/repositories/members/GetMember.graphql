// TODO: activityから外部キーでmemberを取得したい
query GetMemberStatusActivitiesTestLatest($status: String!) {
  member_status_activities_test(
    order_by: { created_at: desc }
    limit: 1
    where: { status: { _eq: $status } }
  ) {
    created_at
    member_id
    status
    id
  }
}
